# Gradle
.gradle
build
captures
.externalNativeBuild
.cxx

# Generated API docs (built by CI, not committed)
website/api

# Kotlin
.kotlin

# IDE
*.iml
.idea

# OS
.DS_Store

# Android / local config (may contain SDK paths or secrets)
local.properties

# Apple / Xcode / CocoaPods
xcuserdata
Pods
*.podspec
DerivedData
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
*.xcworkspace/*
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings

# Node (website/scripts tooling)
node_modules/

# Claude Code runtime/session state (machine-local, not shared config)
.claude/

# Signing & secrets — never commit private keys, credentials, or provisioning material
*.gpg
*.jks
*.keystore
*.p12
*.p8
*.pem
*.cer
*.mobileprovision
keystore.properties
secring.gpg
.env
.env.*
*.env
!.env.example
